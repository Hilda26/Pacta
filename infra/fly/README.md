# Optional Fly.io Template

This directory is retained only as an optional deployment template. Pacta does not have an approved backend hosting provider yet. Do not deploy the backend to Fly.io unless the user explicitly approves Fly.io during the deployment phase.
